# Environment variables (secrets, API keys, DB credentials)
# Ignore ALL .env files except .env.example
.env*
.env.example
# Dependencies
node_modules/

# Uploaded user documents (PII — never push)
uploads/

# SQL migration files (schema exposure)
src/config/*.sql
*.sql

# Migration scripts
run_migration.js
migrate_*.js

# OS files
.DS_Store
Thumbs.db

# Build output
dist/
build/